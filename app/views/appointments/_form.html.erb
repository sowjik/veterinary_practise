<%= form_for(@appointment) do |f| %>
  <% if @appointment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@appointment.errors.count, "error") %> prohibited this appointment from being saved:</h2>

      <ul>
      <% @appointment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date_of_visit %><br>
    <%= f.text_field :date_of_visit, :class => "datepicker", :required =>true %>
  </div>
  <div class="field">
    <%= f.label :pet %><br>
    <%= f.text_field :pet, :required =>true %>
  </div>
  <div class="field">
    <%= f.label :customer %><br>
    <%= f.text_field :customer, :required =>true %>
  </div>
  <div class="field">
    <%= f.label :reason %><br>
    <%= f.text_area :reason, :required =>true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


   
<script>
    $(function() {
    $( ".datepicker" ).datepicker({ minDate: 0});
  });
</script>